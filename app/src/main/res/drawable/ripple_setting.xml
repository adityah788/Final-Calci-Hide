<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Ripple color -->

    <!-- Ripple effect mask with corner radius -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#80FFFFFF" /> <!-- Semi-transparent ripple color -->
            <corners android:radius="15dp" /> <!-- Adjust corner radius if needed -->
        </shape>
    </item>
</ripple>
